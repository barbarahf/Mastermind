//package com.company;
//
//import java.util.*;
//
//public class WTF {
//    public static void main(String[] args) {
////        int max = 7;
////        int numbersNeeded = 5;
////        if (max < numbersNeeded) {
////            throw new IllegalArgumentException("Can't ask for more numbers than are available");
////        }
////        Random rng = new Random();
////        Set<Character> generated = new LinkedHashSet<Character>();
////        while (generated.size() < numbersNeeded) {
////            Character next = rng.nextInt(max) + 1;
////            generated.add(next);
////        }
////        System.out.println(generated);
//
//        char[] lletres = {'A', 'B', 'C', 'D', 'E', 'F', 'G', 'H',};
//        int quantes = 4;
//
//        int max = lletres.length;
//        int numbersNeeded = quantes;
//        if (max < numbersNeeded) {
//            throw new IllegalArgumentException("No puedes pedir más letras de las disponibles");
//        }
//        Random random = new Random();
//        Set<Integer> generado = new LinkedHashSet<Integer>();
//        while (generado.size() < numbersNeeded) {
//            Integer next = random.nextInt(max) + 1;
//            Character holu = random.nextInt(max)+1;
//            generado.add(next);
//        }
//
//
////        String[] itemsArray = new String[generado.size()];
////        itemsArray = generado.toArray(itemsArray);
//        System.out.println(generado);
//    }
//}
